spring:
  application:
    name: XYZ_company_management
  datasource:
    url: jdbc:postgresql://localhost:5432/xyz_company_management
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect